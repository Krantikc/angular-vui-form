import { VuiInputDirective } from './vui-input.directive';

describe('VuiInputDirective', () => {
  it('should create an instance', () => {
    const directive = new VuiInputDirective();
    expect(directive).toBeTruthy();
  });
});
